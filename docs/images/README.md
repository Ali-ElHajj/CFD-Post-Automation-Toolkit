# Screenshot placeholders

No screenshots were provided with the source package. Add real captures here:

1. `01-mode-selection.png` — the three operating modes.
2. `02-thermal-and-variables.png` — comfort settings and custom CEL variables.
3. `03-visual-selection.png` — surface/section variable and range selections.
4. `04-histograms.png` — independent surface/volume histogram selections.
5. `05-average-table.png` — surface/volume averages.
6. `06-cfx-result.png` — a representative contour in CFX-Post.

Use readable captures and remove private filenames/case details. Once images exist, replace the README placeholder table with Markdown images such as:

```markdown
![Choose an operating mode](docs/images/01-mode-selection.png)
```

That path is relative to the root README. The repository ignore rules allow these images to be committed.
